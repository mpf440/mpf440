#include <stdio.h>
#include <math.h>

int main()
{
    int a,b,res;

    printf("el valor de a es:");
    scanf("%d",&a);
    printf("el valor de b es:");
    scanf("%d",&b);

    res=pow(a,b);
    return 0;
}